import { Body } from "@/components/typography/Body";

export function SlidingWindowContent() {
  return (
    <Body>
      Sliding window is a technique for solving problems by iterating over an
      array with a fixed-size window.
    </Body>
  );
}
