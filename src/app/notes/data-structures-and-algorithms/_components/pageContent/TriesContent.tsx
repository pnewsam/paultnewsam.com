import { Body } from "@/components/typography/Body";

export function TriesContent() {
  return (
    <Body>
      Tries are a tree-like data structure that store strings in a compact way.
    </Body>
  );
}
